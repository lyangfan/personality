# DeepMemory 环境变量配置模板
# 复制此文件为 .env 并填入你的 API Key

# ==================== API 配置 ====================
# ⭐ 对话 API Key（GLM-4 Flash）
# 用于 AI 对话和记忆提取
GLM_API_KEY=your-glm-4-api-key-here

# ⭐ Embedding API Key（Embedding-3）
# 用于语义检索和向量化（可选，如果不设置会使用 GLM_API_KEY）
GLM_EMBEDDING_API_KEY=your-embedding-api-key-here

# 可选：OpenAI API Key（如果使用 OpenAI embeddings）
# OPENAI_API_KEY=your-openai-api-key-here

# ==================== 环境配置 ====================
# 运行环境: production | development
# production 模式强制使用 glm 或 sentence-transformers embedding
ENVIRONMENT=production

# ==================== Embedding 配置 ====================
# ⭐ Embedding 模型选择
# simple: 简单编码（免费、快速，仅限开发环境）
# glm: 智谱 Embedding-3（高质量，生产环境推荐）
# sentence-transformers: 多语言模型（需要下载模型文件）
# openai: OpenAI embeddings（需要 API Key）
EMBEDDING_MODEL=glm

# ==================== 存储配置 ====================
# 数据目录
DATA_DIR=./data

# ChromaDB 持久化目录
CHROMADB_DIR=./data/chromadb

# 用户数据目录
USERS_DIR=./data/users

# 会话数据目录
SESSIONS_DIR=./data/sessions

# ==================== 对话配置 ====================
# 记忆提取阈值（每 N 轮对话提取一次记忆）
MEMORY_EXTRACT_THRESHOLD=5

# 最大上下文记忆数（注入到 Prompt 的记忆数量）
MAX_CONTEXT_MEMORIES=5

# ==================== 服务配置 ====================
# 服务监听地址
HOST=0.0.0.0

# 服务监听端口
PORT=8000

# 工作进程数（生产环境建议使用多进程）
WORKERS=4

# 是否自动重载（开发模式）
RELOAD=false
